# PD.md — 产品需求文档
> 群星对话 · dialogue-with-stars
> 最后更新：2026-03-22

---

## 1. 项目定位

**一句话描述**：让用户与历史上的伟大哲学家进行深度对话，通过逐步探索哲学思想，解锁知识卡片与更多哲学家。

**目标用户**：对哲学感兴趣但缺乏系统学习路径的普通用户（非学术研究者）。

**使用场景**：公开上线的 Web 应用，用户无需注册/填写 Key 即可直接使用，AI 调用走后端代理，Key 由开发者统一管理。

---

## 2. 页面清单

| 页面 | 组件 | 说明 |
|------|------|------|
| 星空主页 | `StarMap` + `StarField` + `Nebula` | 哲学家以星星形式分布，点击进入对话 |
| 哲学家介绍页 | `PhilosopherIntro` | 进入对话前的人物简介过渡页 |
| 对话页 | `ChatView` + `ChatBubble` + `SuggestedQuestions` + `OutlineProgress` | 核心对话界面 |
| 思维导图弹窗 | `ThoughtMapModal` | 展示哲学家的思想节点图谱 |
| 知识卡片弹窗 | `KnowledgeCardModal` | 触发后弹出的知识卡片详情 |
| 解锁通知 | `UnlockBanner` | 满足条件时弹出新哲学家解锁提示 |
| ~~API 配置页~~ | ~~`ApiConfigScreen`~~ | ~~已废弃~~ — 改为后端统一管理 Key |

---

## 3. 功能列表与验收标准

### F1：星图展示与导航
- **功能**：星空背景上显示 9 位哲学家星星，锁定的显示为暗淡状态，点击已解锁的进入介绍页
- **验收标准**：
  - 已解锁哲学家（苏格拉底默认解锁）可点击，点击后进入介绍页
  - 未解锁哲学家点击无响应（或显示解锁条件 tooltip）
  - 星图间有星座连线，连线根据 `CONSTELLATIONS` 数据渲染

### ~~F2：API 配置~~（已废弃，改为 F2：后端代理）
- **功能**：前端不再暴露任何 API Key，所有 AI 请求经由后端代理转发至 DeepSeek
- **验收标准**：
  - 前端调用 `POST /api/chat`，后端注入 Key 后转发 DeepSeek
  - Key 仅存于服务器 `.env`，从不出现在前端代码或响应体中
  - `ApiConfigScreen.tsx` 组件删除，`ApiConfig` 相关 Zustand 状态清理
  - 用户打开页面即可直接对话，无需任何配置步骤

### F3：对话引擎
- **功能**：以对应哲学家的人格和 system prompt 驱动 AI 对话，根据对话阶段动态调整策略
- **验收标准**：
  - 对话阶段随 turnCount 递进：rapport(0-2) → clarify(3-4) → test(5-7) → deepen(8+)
  - 每次发送消息后显示 loading 状态，收到回复后消失
  - AI 回复中含"思维坐标"标注（`isCoordinate: true` 的消息）时以特殊样式渲染
  - AI 回复显示内容中**不含** `[NODE:xxx]` 字样（tag 由前端解析后剥除）
  - **对话历史仅在当前 session 保留，刷新后重置**（进度数据持久化，对话本身不持久）
  - 每 5 轮触发一次"思维坐标"旁白（`isCoordinate: true`），由独立 prompt 生成

### F4：思想节点匹配与探索
- **功能**：AI 在回复末尾主动追加 `[NODE:id]` 标签作为主要匹配信号，前端关键词评分作为兜底；命中节点后记录 hitCount，达到阈值则标记"已深入"
- **验收标准**：
  - AI 回复末尾含 `[NODE:id]`，前端正则 `/\[NODE:(\w+)\]/` 解析，tag 从显示文本中剥除
  - AI 标签优先于关键词匹配；若 AI 未标签或标签节点不存在，则退回关键词分数 ≥ 3 的节点
  - 每次命中节点：`nodeHitCounts[nodeId] +1`
  - hitCount = 1：触发底部"已触及"通知（NodeTouchNotice）
  - hitCount ≥ 3：节点标记为"已深入"（exploredNodes），同时触发知识卡片
  - 已深入节点在思维导图中显示"📖 查看知识卡片"按钮，可随时调出
  - exploredNodes 跨 session 持久化；nodeHitCounts 仅 session 内有效
  - OutlineProgress 显示已深入节点数 / 总节点数

### F5：知识卡片三阶段系统
- **功能**：通过 hitCount 追踪节点命中深度，三个阶段给予不同反馈
- **验收标准**：
  - **hitCount = 1（第一次触及）**：底部滑出 NodeTouchNotice 通知"💡 你触及了新的思想节点：xxx"，3.5 秒自动消失，可点击提前关闭
  - **hitCount = 2（积累期）**：无动作
  - **hitCount ≥ 3（深入）**：KnowledgeCardModal 从屏幕底部滑入（非居中弹窗），同时写入 exploredNodes + collectedCards
  - 已深入节点不再重复弹出卡片
  - 卡片内容来自 ThoughtNode.card 字段（icon、title、subtitle、content、source）
  - 卡片关闭后，思维导图中该节点显示"📖 查看知识卡片"按钮可再次调出

### F6：哲学家解锁系统
- **功能**：收集足够卡片或对话轮次后解锁新哲学家
- **验收标准**：
  - 柏拉图：需 collectedCards >= 3 AND chatCount >= 6
  - 满足条件时显示 UnlockBanner 通知
  - 解锁后星图对应星星变亮，可点击进入

### F7：推荐问题
- **功能**：对话页下方展示最多 3 个推荐问题（含"查看思想脉络"入口），点击直接发送
- **验收标准**：
  - 每次 AI 回复后异步生成推荐问题（300ms 延迟）
  - **所有展示给用户的问题在展示时即标记为已用**（不管是否点击），不会在同 session 内重复出现
  - 问题显示格式：最多 2 条文字问题 + 1 个固定"🗺 查看思想脉络"入口
  - 问题级别随节点命中深度提升：未触及→宏观问题，已触及→中层问题，已深入→微观问题
  - usedQuestions 仅 session 内有效，刷新后重置（重新开始一次新的对话旅程）

### F8：进度重置
- **功能**：用户可以重置全部进度（探索节点、卡片收藏、对话历史）
- **验收标准**：
  - 重置后回到初始状态，仅苏格拉底解锁

---

## 4. 哲学家列表

### 第一批（前期开发，共 8 位）

| # | ID | 姓名 | 时代 | 解锁条件 |
|---|-----|------|------|---------|
| 1 | socrates | 苏格拉底 | 469-399 BC | 默认解锁 |
| 2 | plato | 柏拉图 | 428-348 BC | 收藏卡片 ≥ 3，对话轮次 ≥ 6 |
| 3 | aristotle | 亚里士多德 | 384-322 BC | 待定 |
| 4 | epicurus | 伊壁鸠鲁 | 341-270 BC | 待定 |
| 5 | zeno | 芝诺（斯多葛） | 334-262 BC | 待定 |
| 6 | augustine | 圣奥古斯丁 | 354-430 | 待定 |
| 7 | aquinas | 阿奎那 | 1225-1274 | 待定 |
| 8 | kant | 康德 | 1724-1804 | 待定 |

### 远期规划（~40 位，UI 显示"待解锁"占位）

| 姓名 | 时代 | 批次 |
|------|------|------|
| 黑格尔 | 1770-1831 | 二 |
| 叔本华 | 1788-1860 | 二 |
| 马克思 | 1818-1883 | 二 |
| 尼采 | 1844-1900 | 二 |
| 笛卡尔 | 1596-1650 | 二 |
| 胡塞尔 | 1859-1938 | 三 |
| 罗素 | 1872-1970 | 三 |
| 维特根斯坦 | 1889-1951 | 三 |
| 海德格尔 | 1889-1976 | 三 |
| 萨特 | 1905-1980 | 三 |
| 波伏瓦 | 1908-1986 | 三 |
| 卡尔·波普尔 | 1902-1994 | 三 |
| 蒯因 | 1908-2000 | 三 |
| 福柯 | 1926-1984 | 三 |
| ...（后续补充至约 40 位） | | 四+ |

> 星图 UI 需为远期哲学家预留位置，以暗淡/虚影样式展示"即将到来"。

---

## 5. 非功能需求

- **部署**：公开上线，前端静态文件 + 后端 Node.js 服务（同域或代理）
- **AI 服务商**：固定使用 DeepSeek（`deepseek-chat` 模型），不再支持用户自选
- **用户体系**：分阶段实施
  - **Phase 1-2（验证期）**：游客模式，无需注册，数据存浏览器 IndexedDB
  - **Phase 3+（验证通过后）**：手机号+验证码 / 微信登录，数据迁移至服务端（对话历史、进度、解锁状态、收藏卡片）
- **数据隐私**：验证期间用户对话经后端转发至 DeepSeek，不做服务端落地存储
- **安全**：
  - API Key 仅存服务器 `.env`，绝不出现在前端代码、响应体、日志中
  - 后端需做基本限流防止 Key 被滥用（rate limit）
- **商业化**：验证期不考虑，待产品验证通过后设计付费模型
- **成本**：DeepSeek 按 token 计费，上线初期不设上限，后续视流量加限流

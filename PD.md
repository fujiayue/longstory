# PD.md — 产品需求文档
> 群星对话 · dialogue-with-stars
> 最后更新：2026-03-21

---

## 1. 项目定位

**一句话描述**：让用户与历史上的伟大哲学家进行深度对话，通过苏格拉底式问答逐步探索哲学思想，解锁知识卡片与更多哲学家。

**目标用户**：对哲学感兴趣但缺乏系统学习路径的普通用户（非学术研究者）。

**使用场景**：个人自用，本地运行，用户自备 API Key，不涉及用户注册/数据上传。

---

## 2. 页面清单

| 页面 | 组件 | 说明 |
|------|------|------|
| 星空主页 | `StarMap` + `StarField` + `Nebula` | 哲学家以星星形式分布，点击进入对话 |
| 哲学家介绍页 | `PhilosopherIntro` | 进入对话前的人物简介过渡页 |
| 对话页 | `ChatView` + `ChatBubble` + `SuggestedQuestions` + `OutlineProgress` | 核心对话界面 |
| 思维导图弹窗 | `ThoughtMapModal` | 展示哲学家的思想节点图谱 |
| 知识卡片弹窗 | `KnowledgeCardModal` | 触发后弹出的知识卡片详情 |
| 解锁通知 | `UnlockBanner` | 满足条件时弹出新哲学家解锁提示 |
| API 配置页 | `ApiConfigScreen` | 首次使用时填写 API Key 和模型配置 |

---

## 3. 功能列表与验收标准

### F1：星图展示与导航
- **功能**：星空背景上显示 9 位哲学家星星，锁定的显示为暗淡状态，点击已解锁的进入介绍页
- **验收标准**：
  - 已解锁哲学家（苏格拉底默认解锁）可点击，点击后进入介绍页
  - 未解锁哲学家点击无响应（或显示解锁条件 tooltip）
  - 星图间有星座连线，连线根据 `CONSTELLATIONS` 数据渲染

### F2：API 配置
- **功能**：用户首次使用时填写 API Key、选择 Provider（Anthropic/DeepSeek/OpenAI/自定义）
- **验收标准**：
  - 配置保存后持久化（刷新页面后仍存在）
  - 支持的 Provider：Anthropic、DeepSeek、OpenAI、自定义 OpenAI 兼容
  - 未配置 API Key 时不能进入对话页
  - API Key 存储在 IndexedDB（不上传任何服务器）

### F3：对话引擎
- **功能**：以对应哲学家的人格和 system prompt 驱动 AI 对话，根据对话阶段动态调整策略
- **验收标准**：
  - 对话阶段随 turnCount 递进：rapport(0-2) → clarify(3-4) → test(5-7) → deepen(8+)
  - 每次发送消息后显示 loading 状态，收到回复后消失
  - AI 回复中含"思维坐标"标注（`isCoordinate: true` 的消息）时以特殊样式渲染
  - 对话历史刷新后不丢失（IndexedDB 持久化）

### F4：思想节点匹配与探索
- **功能**：对话引擎根据用户消息关键词匹配哲学家的思想节点（ThoughtNode），标记为"已探索"
- **验收标准**：
  - 匹配到新节点时，思维导图中该节点变为已探索状态
  - 探索记录跨会话持久化
  - OutlineProgress 组件显示已探索节点数/总节点数

### F5：知识卡片触发
- **功能**：满足条件时弹出知识卡片（KnowledgeCardModal）
- **验收标准**：
  - 触发条件：匹配到新节点 AND turnCount >= 3 AND (turnCount - lastCardTurn) >= 3
  - 弹出后用户可选择"收藏"或"关闭"
  - 收藏后记入 `collectedCards`，不再重复弹出同一节点的卡片
  - 知识卡片内容来自 ThoughtNode.card 字段

### F6：哲学家解锁系统
- **功能**：收集足够卡片或对话轮次后解锁新哲学家
- **验收标准**：
  - 柏拉图：需 collectedCards >= 3 AND chatCount >= 6
  - 满足条件时显示 UnlockBanner 通知
  - 解锁后星图对应星星变亮，可点击进入

### F7：推荐问题
- **功能**：对话页下方展示 3 个 AI 生成的建议问题，点击自动填入输入框
- **验收标准**：
  - 每次 AI 回复后异步生成新的推荐问题
  - 已使用过的问题不再推荐（`usedQuestions` Set 去重）
  - 推荐问题与当前对话上下文相关

### F8：进度重置
- **功能**：用户可以重置全部进度（探索节点、卡片收藏、对话历史）
- **验收标准**：
  - 重置后回到初始状态，仅苏格拉底解锁
  - API 配置不受重置影响

---

## 4. 哲学家列表（当前规划）

| ID | 姓名 | 解锁条件 |
|----|------|---------|
| socrates | 苏格拉底 | 默认解锁 |
| plato | 柏拉图 | 收藏卡片 ≥ 3，对话轮次 ≥ 6 |
| aristotle | 亚里士多德 | 待定 |
| epicurus | 伊壁鸠鲁 | 待定 |
| zeno | 芝诺 | 待定 |
| augustine | 奥古斯丁 | 待定 |
| descartes | 笛卡尔 | 待定 |
| kant | 康德 | 待定 |
| nietzsche | 尼采 | 待定 |

---

## 5. 非功能需求

- **部署**：本地运行（`npm run dev`），不部署到公网
- **数据隐私**：API Key 仅存 IndexedDB，不发送到任何第三方服务（除用户配置的 AI 服务商）
- **性能**：对话响应依赖外部 API，无特别性能要求
- **安全**：无用户账户体系，无后端，无安全合规要求

# project.md — 项目执行计划与状态追踪
> 群星对话 · dialogue-with-stars
> 最后更新：2026-03-22

---

## 总览

- **目标产品**：面向公众发布的哲学对话 App（iOS / Android / Web）
- **第一批哲学家**：苏格拉底（已完成）+ 柏拉图、亚里士多德、伊壁鸠鲁、芝诺、奥古斯丁、阿奎那、康德（共 8 位）
- **远期规模**：约 40 位哲学家，分批上线
- **AI 服务**：DeepSeek（后端代理，Key 不暴露给用户）
- **用户体系**：Phase 1-2 游客模式（先验证产品）→ Phase 3 手机号/微信登录 + 数据云同步
- **商业化**：验证通过后再设计，不提前投入
- **整体周期预估**：20-24 周（5 个 Phase）

---

## Phase 1：架构升级与内容管线建立（第 1-4 周）

### 第 1 周：工程化迁移 ✅ 已完成

**目标：把 v0.7 的单文件原型迁移成正式项目，功能完全一致，不增不减。**

- [x] Day 1-2：Vite + React + TypeScript 项目初始化，目录结构搭建
- [x] Day 3-4：组件拆分（14 个组件从单文件迁出），TypeScript 类型同步补全
- [x] Day 5：核心类型系统定义（PhilosopherModule, ThoughtNode, AppState 等接口）
- [x] Git 初始化 + 初始提交
- [x] PD.md / 2K.md / project.md 文档创建

产出：项目 `npm run dev` 可跑，功能与 v0.7 一致，56 个文件全部纳入版本管理。

---

### 第 2 周：后端代理搭建 ✅ 已完成

**目标：搭建 Express 后端代理，前端不再暴露任何 API Key，用户打开即可对话。**

**Day 1-2：后端骨架**

具体操作：
- 在项目根目录创建 `server/` 目录
- 初始化 Express + TypeScript 后端：

```
server/
  index.ts          # 入口：Express 启动、CORS、中间件
  routes/
    chat.ts         # POST /api/chat → 转发 DeepSeek
  middleware/
    rate-limit.ts   # 基础限流（防止 Key 被滥用）
```

- 安装依赖：`express`、`dotenv`、`cors`
- `.env` 存放 `DEEPSEEK_API_KEY`（已 gitignore）
- Vite 开发模式下配置 proxy 指向后端（`vite.config.ts` 添加 proxy 规则）

注意事项：
- 后端不做任何用户数据存储，只做请求转发
- 请求体格式：`{ messages: ChatMessage[], philosopher_id: string }`
- 后端负责注入 API Key、拼装 DeepSeek 请求、返回响应
- 生产环境需考虑部署方式（同域部署 or Nginx 反向代理）

产出：`npm run dev` 同时启动前后端，前端调 `/api/chat` 可成功获取 AI 回复。

**Day 3-4：前端 AI 调用层改造**

具体操作：
- 重写 `src/engine/ai-client.ts`：
  - 删除所有 Provider 切换逻辑（Anthropic / OpenAI-compat）
  - 删除 API Key 读取逻辑
  - 改为单一的 `POST /api/chat` 调用
- 删除 `ApiConfigScreen.tsx` 组件
- 清理 Zustand store 中的 `apiConfig`、`apiReady` 相关字段
- 清理 `src/types/philosopher.ts` 中的 `ApiConfig`、`ProviderPreset` 类型
- 清理 `src/data/philosophers-meta.ts` 中的 `PROVIDER_PRESETS`
- 修改 `App.tsx`：去掉 API 配置流程，用户进入后直接看到星图

注意事项：
- 逐个文件改，每改一个跑一次确认无报错
- 确认删除后 `npm run build` 无 TypeScript 错误
- 确认 IndexedDB 中不再存储任何 API Key 相关数据

产出：前端代码中零 API Key 引用。用户打开页面直接看到星图，无需任何配置步骤。

**Day 5：端到端验证**

具体操作：
- 完整走通流程：打开页面 → 点击苏格拉底 → 进入介绍页 → 开始对话 → 收到 AI 回复
- 验证持久化：刷新页面后对话历史仍在
- 验证错误处理：关闭后端后前端是否有合理的错误提示
- 验证限流：连续快速发送 10 条消息，后端限流是否生效

产出：后端代理完整可用，提交 Git。

---

### 第 3 周：持久化验证 + 数据模块解耦

**目标：确认持久化完全生效、数据驱动架构真正解耦、为多哲学家做好准备。**

**Day 1-2：持久化完整验证**

具体操作：
- 逐一验证以下场景刷新后数据不丢：
  - 对话历史（messages）
  - 已探索节点（exploredNodes）
  - 已收藏卡片（collectedCards）
  - 对话轮次（chatCount）
  - 已使用问题（usedQuestions → Set 序列化/反序列化）
  - 哲学家解锁状态（philosophers）
- 验证 Set<string> 的 IndexedDB 序列化：存为数组，读回转 Set，不丢数据
- 验证首次加载（IndexedDB 为空）时默认值兜底正常

产出：持久化 100% 生效，刷新无数据丢失。

**Day 3-4：数据模块化验证**

具体操作：
- 确认苏格拉底数据按 PhilosopherModule 接口完整整理到 `data/philosophers/socrates/`
- 验证 `loadPhilosopher(id)` 函数正确加载
- 把 ChatView、ThoughtMapModal、对话引擎等所有消费数据的地方，确认是从 PhilosopherModule 读取，不再硬引用全局变量
- 验证方法：修改苏格拉底某个 ThoughtNode 的 label，确认 UI 自动反映变化
- 对话历史按哲学家 id 分开存储（切换哲学家时各自独立）

产出：苏格拉底模块完全数据驱动，新增哲学家 = 新增数据文件 + 零代码修改。

**Day 5："即将到来"占位 UI**

具体操作：
- 在 `philosophers-meta.ts` 中添加远期哲学家的占位数据（名字、时代、坐标），标记 `status: 'coming-soon'`
- 修改 StarMap 组件：
  - 已解锁：正常亮度，可点击
  - 未解锁（有内容）：暗淡，显示解锁条件
  - 即将到来（无内容）：虚影/半透明样式，hover 显示"即将到来"
- 修改星座连线：远期哲学家之间的连线用虚线

注意事项：
- 远期哲学家列表参见 PD.md 第 4 节，当前约 20 位，最终目标 ~40 位
- 坐标布局需预留空间，不能把 8 位塞满整个星图

产出：星图显示约 20+ 颗星星，大部分是"即将到来"的虚影，给用户期待感。

---

### 第 3 周：持久化验证 + 数据模块解耦 ✅ 已完成

- [x] IndexedDB 持久化验证（exploredNodes / collectedCards / chatCount 跨 session）
- [x] 对话历史改为 session 级别（不持久化，刷新后重置）
- [x] PhilosopherModule 数据驱动架构验证（苏格拉底完整模块）
- [x] "即将到来"占位 UI（星图显示远期哲学家虚影）

---

### 第 4 周：内容规范 + 柏拉图启动 ✅ 主体完成（解锁Bug待修）

**已完成（Week 4 实际产出）**：
- [x] 柏拉图完整模块：9个思想节点（cave/eidos/the_good/anamnesis/tripartite_soul/justice/philosopher_king/eros_ascent/immortality）
- [x] 柏拉图 system prompt（~2000字，含角色、教学法、[NODE:id]标注指令、示例对话）
- [x] 柏拉图 translations/frameworks/lineage 数据
- [x] 卡片系统重构（P0-P4，见下）
- ⚠️ **Plato 解锁 Bug 待修**：苏格拉底全探索后柏拉图仍显示锁定，原因未定位

**卡片系统重构（P0-P4）** ✅ 已完成，TypeScript 零错误：
- P0：推荐问题全展示即标记已用，session 内不重复
- P1：AI prompt 追加 [NODE:id] 标注指令，ChatView 解析 + 剥除 tag
- P2：统一卡片触发（首次触及即弹出 KnowledgeCardModal 卡片弹窗，移除 NodeTouchNotice 组件）
- P3：ThoughtMapModal 已探索节点增加"📖 查看知识卡片"按钮
- P4：推荐问题级别随节点命中深度提升（宏观→中层→微观）

---

**Day 1-2：《哲学家内容模块制作手册》定稿**

手册必须包含：
- 字段清单：逐个列出 PhilosopherModule 每个字段，含义、字数要求、格式要求、质量标准，附苏格拉底实例
- domainIntro 写作规范：第一人称、有具体故事/类比/论证、以引人深思的问题结尾、300-500 字、不能写成百科词条
- system prompt 写作规范：约 1500-2500 字，含核心身份、根本任务、思维方法工具箱、教学节奏、分场景规则、语言风格、禁止事项、示例对话（6-8 组）
- 知识卡片规范：content ≤ 150 字、source 精确到篇名和章节号
- reframingRules 规范：userSays 用日常口语、youHear 用哲学概念框架重命名
- 对话测试规范：每位哲学家至少 20 轮测试，记录跳戏次数、卡片触发准确率
- 原始文献要求：注明哪些观点必须有原文依据，哪些可以合理推演

产出：一份可以直接交给内容编写者的操作手册。

**Day 3-4：柏拉图思想节点架构设计**

具体操作：
- 基于《理想国》《会饮篇》《斐多篇》《美诺篇》梳理 8-10 个节点
- 预估节点：理念论、洞穴寓言、灵魂三分、回忆说、哲人王、善的理念、爱的阶梯、灵魂不朽
- 画出节点关系图和推荐探索路径
- 写出每个节点的 id、label、brief、icon、color、description、keywords
- 不写 domainIntro 和 prompt——只做骨架

产出：柏拉图节点架构文档（节点清单 + 关系图 + 推荐路径）。

**Day 5：柏拉图 domainIntro 初稿（前 3 个核心节点）**

写作要点：
- 柏拉图口吻 ≠ 苏格拉底口吻。苏格拉底是「我不知道，我们一起想」，柏拉图是「让我告诉你我看到了什么」
- 善用寓言和宏大叙事——洞穴寓言要让用户身临其境
- 会引用老师苏格拉底，但明确说出自己走得更远的地方

产出：理念论、洞穴寓言、灵魂三分 3 段 domainIntro 初稿。

---

## Phase 1 验收标准

| # | 验收项 | 具体标准 |
|---|--------|---------|
| 1 | 工程可运行 | `npm run dev` 前后端同时启动无报错 |
| 2 | 后端代理可用 | 前端调 `/api/chat` 可获得 DeepSeek 回复，Key 不出现在前端 |
| 3 | 功能一致性 | 与 v0.7 全部功能逐项对比（除去 ApiConfigScreen），无遗漏无退化 |
| 4 | TypeScript 零报错 | `tsc --noEmit` 通过 |
| 5 | 持久化生效 | 刷新后对话历史、卡片、进度全部恢复 |
| 6 | 数据驱动 | 新建测试哲学家目录 → 不改代码 → App 自动识别可对话 |
| 7 | "即将到来" UI | 星图显示远期哲学家虚影占位 |
| 8 | 内容手册可交付 | 手册交给不了解项目的人，对方能独立理解每个字段该写什么 |
| 9 | 柏拉图骨架 | 苏格拉底解锁柏拉图 → 进入对话 → 查看脉络图，全流程无报错 |

---

## Phase 2：内容生产·第一批（第 5-14 周）

这是最核心、耗时最长的阶段。内容质量决定产品命运。

**生产节奏**：每位哲学家 2 周一个完整周期（研读→编写→调试→审核）。思想体系较精简的可以在同一周期并行。

### 第 5-6 周：柏拉图模块完成 + 对话引擎通用化

#### 内容线：柏拉图

**Day 1-3：文献精读 + 剩余节点编写**
- 精读：《理想国》卷五至卷七（哲人王、洞穴、线段、太阳）、《斐多篇》（灵魂不朽四论证）、《美诺篇》（回忆说）、《会饮篇》
- 补齐剩余 5-7 个节点的全部字段
- 每个节点按手册逐字段填写：domainIntro、coreQuestions、keywords、三层问题池、bridgeTemplates、reframingRules、teachingHints、biographicalRefs、avoidWhen、commonMisuse、知识卡片

柏拉图口吻要点：
- 比苏格拉底更系统、更有构建体系的野心、善用寓言
- 善用「分有」关系解释个别与普遍
- 核心张力：可见世界 vs 可知世界、意见 vs 知识、影子 vs 实在
- 会说「让我给你讲一个故事」

**Day 4-5：system prompt 编写**
- 约 1500-2000 字，含：核心身份、思维方法工具箱（辩证上升、寓言构建、二分世界、师承引用、灵魂类比）、禁止事项、示例对话 6-8 组

**Day 6-7：现代话题桥接 + 脉络数据**
- 5-7 条 TRANSLATION_FRAGMENTS（社交媒体→洞穴影子、AI→模仿vs真知、教育→灵魂转向、政治→哲人王、艺术→模仿的模仿）
- DEEP_FRAMEWORKS、脉络星座图坐标/连线、推荐顺序

**Day 8-10：调试 + 审核**
- 至少 20 轮完整对话测试
- 测试场景：自由聊天引出核心思想、直接问理念论、挑战理想国不现实、聊到苏格拉底的师承、现代话题桥接、15 轮以上连续对话不跳戏

#### 技术线（并行）
- 完成通用对话引擎：prompt 按哲学家 id 切换
- 完成通用思想脉络星座图组件：数据驱动渲染
- 解锁动画与过渡效果

产出：柏拉图完整可对话，从苏格拉底解锁 → 进入对话 → 全流程可走通。

---

### 第 7-8 周：亚里士多德

**方法论**：分类与分析——对任何问题先做分类，再逐一考察。三段论推理。
**独特气质**：经验主义者，尊重具体事实。会明确反驳柏拉图：「吾爱吾师，吾更爱真理」。
**核心张力**：形式 vs 质料、潜能 vs 现实、过度 vs 不及。
**对话特征**：喜欢说「在某种意义上……但在另一种意义上……」，总是做区分。

预估 9-11 个节点。参考文献：《尼各马可伦理学》《形而上学》《物理学》《政治学》《论灵魂》。

节奏同柏拉图：研读→节点编写→prompt→桥接→调试，2 周完成。

---

### 第 9-10 周：伊壁鸠鲁 + 芝诺（并行）

两人思想体系相对精简（各 6-8 节点），安排在同一周期并行。

**伊壁鸠鲁**
- 方法：欲望分类法——自然必要 / 自然非必要 / 非自然非必要
- 气质：温和、亲切、朋友般的语气。不追问不反驳，轻轻指向简单生活
- 核心张力：快乐 vs 放纵、有限 vs 无限欲望、死亡 vs 存在
- 对话特征：语速慢，用词简朴，常用食物和花园做比喻
- 参考文献：《致美诺西书》《致希罗多德书》《主要学说》、第欧根尼·拉尔修记述

**芝诺（斯多葛）**
- 方法：控制二分法——区分「在我们控制之内的」和「不在我们控制之内的」
- 气质：坚毅、冷静、军人般的纪律感。不回避痛苦，直面现实
- 核心张力：自由 vs 命运、理性 vs 激情、内在 vs 外在
- 对话特征：简短有力，善用悖论，不做多余的安慰
- 参考文献：第欧根尼·拉尔修《名哲言行录》、塞涅卡和爱比克泰德作为补充参照

产出：伊壁鸠鲁 + 芝诺双模块完成，共 5 位哲学家可对话。

---

### 第 11-12 周：奥古斯丁 + 阿奎那

**奥古斯丁**
- 方法：忏悔式内省——从自身经历出发反思，用第一人称讲述内心挣扎
- 气质：有过放纵的青年时代，经历过深刻的信仰转变。热烈、情感充沛
- 核心张力：罪与恩典、自由意志 vs 神恩、尘世之城 vs 上帝之城
- 对话特征：会分享自己曾经的迷失，不居高临下。善用《圣经》典故但不说教
- 参考文献：《忏悔录》《上帝之城》《论自由意志》
- 预估 7-9 个节点

**阿奎那**
- 方法：经院辩证法——对每个问题先列反对意见（videtur quod），再给出「相反的说法」（sed contra），最后系统回答（respondeo）
- 气质：冷静、系统、百科全书式的学者。深信信仰与理性终将和解。谦逊但不缺乏自信
- 核心张力：信仰 vs 理性、本质 vs 存在、自然法 vs 人定法、类比 vs 同义
- 对话特征：会说「这个问题，有人主张……但我们必须指出……因此我回答说……」；层层展开，结构严谨
- 参考文献：《神学大全》（重点第一部 Q2 上帝存在五路论证、第二部伦理学部分）、《反异教大全》
- 预估 7-9 个节点

产出：7 位哲学家可对话（苏格拉底 + 6 位新增）。

---

### 第 13-14 周：康德

康德思想体系极为复杂（三大批判），独占 2 周。

**方法论**：先验分析——追问经验得以可能的先决条件，区分现象与物自体。
**气质**：严谨到近乎刻板，从不偏离主题。但对道德法则有宗教般的崇敬。
**核心张力**：自由 vs 自然法则、义务 vs 倾向、现象 vs 物自体。
**对话特征**：句子长且复杂，但每一步都有严格的逻辑。会说「仅仅因为……并不意味着……」。

预估 9-11 个节点，覆盖三大批判核心。参考文献：《纯粹理性批判》《实践理性批判》《判断力批判》《道德形而上学基础》。

产出：第一批全部 8 位哲学家内容完成。

---

### Phase 2 验收标准

| # | 验收项 | 具体标准 |
|---|--------|---------|
| 1 | 8 位哲学家全部可对话 | 每位都能走完：星图点击 → 介绍页 → 对话 → 卡片触发 → 脉络图 |
| 2 | 内容质量 | 每位哲学家 20 轮测试对话中跳戏 ≤ 1 次 |
| 3 | 5 轮教学有效性 | 5 轮对话内至少触达 1 个核心思想节点 |
| 4 | 知识卡片精度 | 每张卡片有原始文献出处（精确到篇名/章节号） |
| 5 | 解锁链路 | 从苏格拉底开始，按设计顺序逐步解锁全部 8 位 |
| 6 | 跨哲学家传承 | 对话中提及师承时，反应合理（如柏拉图谈苏格拉底、亚里士多德评柏拉图） |

---

## Phase 3：App 封装 + 用户体系 + 上线准备（第 15-18 周）

> **前置条件**：Phase 2 的游客版已经验证了产品价值（有人用、有留存），再投入用户体系。如果验证不通过，暂停此阶段，回头调整产品。

### 用户体系（验证通过后实施）
- **注册登录**：手机号+验证码 / 微信登录
- **数据库**：用户表、对话历史表、进度表、收藏表
- **数据同步**：用户登录后，本地 IndexedDB 数据上传至服务端；换设备登录后自动恢复
- **游客→注册迁移**：游客已有的数据在注册后无缝合并，不丢失
- **付费模型设计**：根据验证期数据决定（订阅/买断/免费增值，此阶段确定方案）

### 技术任务
- **Capacitor 集成**：将 Web 应用封装为 iOS / Android App，最大程度复用现有 React 代码
- **后端扩展**：从纯代理升级为完整应用服务器（数据库、鉴权、数据同步 API）
- **后端生产化**：部署到云服务器，配置域名、HTTPS、限流策略
- **UI 适配**：安全区域适配（刘海屏、底部横条）、触觉反馈、原生导航手势
- **离线内容包**：所有思想节点、知识卡片、domainIntro 内置 App，只有 AI 对话需联网
- **深色/浅色模式**：当前为深色，考虑是否需要浅色模式选项
- **"即将到来"完善**：确保远期哲学家占位在 App 中呈现良好

### 产出
- iOS / Android 可安装测试版 App
- 用户注册登录 + 数据云同步
- 后端部署并稳定运行
- 完整端到端用户流程走通

---

## Phase 4：测试与打磨（第 19-21 周）

### 对话质量测试
- 每位哲学家至少 50 轮真实用户测试对话
- 记录跳戏频率和类型
- 记录知识卡片触发时机是否合理
- 记录推荐问题质量
- 重点测试跨哲学家传承

### Prompt 迭代
- 每位哲学家 prompt 预计 3-5 轮修改
- 关注：人物一致性、教学节奏、避免通病（不变成心灵导师、不说「好问题」、不过度列表化）

### App 打磨
- 性能优化：星图动画帧率、大量对话历史滚动流畅度
- 错误处理：API 超时、网络断开的优雅降级
- 无障碍：字体可调、对比度

### 验收标准

| # | 验收项 | 具体标准 |
|---|--------|---------|
| 1 | 跳戏率 | 每位哲学家 50 轮对话跳戏 ≤ 2 次 |
| 2 | 教学有效性 | 5 轮内触达核心节点的比率 ≥ 80% |
| 3 | App 性能 | 星图动画 ≥ 30fps，对话滚动无卡顿 |
| 4 | 错误处理 | 断网/超时场景有明确提示，不白屏不崩溃 |

---

## Phase 5：发布（第 22-24 周）

- Apple App Store 提交（预留 2 周审核时间，首次可能被拒）
- Google Play 提交
- 隐私政策和服务条款撰写
- 应用商店素材：截图、描述文案、关键词优化
- 上架后第一周监控崩溃报告和用户反馈
- 产品官网（可选，用于 SEO 和引流）

---

## 已知问题 & 待决策

| # | 问题 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | ~~v0.7 迁移后功能验证~~ | - | ✅ 已完成 |
| 2 | ~~柏拉图解锁 Bug~~ | - | ✅ 已修复 |
| 3 | 第一批 8 位哲学家的解锁顺序和条件需整体设计 | 高 | 待设计（Week 5 前） |
| 4 | ~~API Key 安全存储~~ | - | ✅ 已决策：后端 .env |
| 5 | 多哲学家对话历史是否按哲学家分开存储 | 中 | 待决策（目前同一 session 共享） |
| 6 | 远期哲学家星图坐标布局（需容纳 ~40 颗星） | 中 | 已有占位，布局待优化 |
| 7 | App 部署平台选择（阿里云/Vercel/Railway/...） | 中 | Phase 3 前决策 |
| 8 | 阿奎那对话方法论补充 | 中 | Week 4 内容手册中 |
| 9 | 用户体系（注册/登录/云同步）延后到 Phase 3 | - | ✅ 已决策 |
| 10 | 商业化模型延后，验证通过后再设计 | - | ✅ 已决策 |
| 11 | 星图节点改为十字星（✦）形状 | 低 | 待实现（视觉调整阶段） |
| 12 | ~~P0-P4 卡片系统重构功能验证~~ | - | ✅ 已完成 |

---

## 质量红线（不可妥协）

1. **哲学准确性**：每个思想节点的核心命题必须有原始文献出处
2. **人物一致性**：连续 20 轮对话跳戏 ≤ 1 次
3. **教学有效性**：5 轮对话内至少触达 1 个核心思想节点
4. **知识卡片精度**：source 精确到篇名和章节号，content ≤ 150 字
5. **不降级发布**：宁可晚发布，不发质量不够的哲学家模块

---

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| 2026-03-21 | 初始化 Git，创建 PD/2K/project.md | 补齐前置规范 |
| 2026-03-21 | 架构改为线上部署 + 后端代理 Key | 面向公众发布，不能暴露 Key |
| 2026-03-21 | 哲学家阵容调整：去掉笛卡尔和尼采，加入阿奎那；前期做到康德 | 用户决策，笛卡尔/尼采移至第二批 |
| 2026-03-21 | 整体计划从 18-22 周调整为 20-24 周 | 新增后端搭建 + 阿奎那内容制作 |
| 2026-03-21 | project.md 全面重写，与执行计划.txt 对齐粒度 | 原版过于粗糙，不符合项目需要 |
| 2026-03-21 | 用户体系和商业化延后到 Phase 3 | 先验证产品价值，再投入用户系统和付费设计 |
| 2026-03-22 | 柏拉图完整模块上线（9节点 + prompt + 数据文件） | Phase 1 Week 4 内容任务 |
| 2026-03-22 | 卡片系统全面重构（P0-P4）：三阶段触发/AI节点标签/问题去重/底部滑入卡片 | 旧系统触发不透明、问题重复、卡片体验差 |
| 2026-03-22 | 对话历史改为 session 级别不持久化 | 用户明确要求：解锁/卡片进度保留，对话每次重开 |
| 2026-03-22 | 补全 PD.md F3/F4/F5/F7 验收标准、2K.md 架构现状、project.md 里程碑记录 | 三份文档与代码严重脱节，补齐步骤 2/7/8 |
| 2026-03-22 | 统一卡片弹窗：NodeTouchNotice 合并进 KnowledgeCardModal，首次触及即弹卡片；doPreset 增加卡片触发逻辑 | 用户反馈两种弹出方式不一致，点击预设问题无法触发卡片 |

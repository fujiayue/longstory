# 2K.md — 架构知识文档
> 群星对话 · dialogue-with-stars
> 最后更新：2026-03-22

---

## 1. 技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | React 19 + TypeScript 5.9 |
| 构建工具 | Vite 8 |
| 状态管理 | Zustand 5（+ persist 中间件） |
| 持久化 | IndexedDB（idb 库封装） |
| 样式 | inline style（暂无 CSS 框架） |
| Lint | ESLint 9 + typescript-eslint |
| **后端** | **Node.js + Express（API 代理服务器）** |
| **AI 服务** | **DeepSeek（`deepseek-chat`，OpenAI 兼容格式）** |
| **Key 管理** | **服务器端 `.env`（`DEEPSEEK_API_KEY`）** |

---

## 2. 目录结构

```
server/              # 后端代理（新增）
  index.ts           # Express 入口，挂载路由
  routes/
    chat.ts          # POST /api/chat → 转发 DeepSeek

src/
  components/        # 纯 UI 组件，不含业务逻辑
    StarField.tsx      # 背景星空粒子效果
    Nebula.tsx         # 星云背景装饰
    StarMap.tsx        # 哲学家星图（SVG）
    ConstellationLines.tsx  # 星座连线
    StarNode.tsx       # 单个哲学家星星节点
    PhilosopherIntro.tsx    # 哲学家介绍页
    ChatView.tsx       # 对话主界面
    ChatBubble.tsx     # 单条消息气泡
    ThoughtMapModal.tsx     # 思维导图弹窗
    KnowledgeCardModal.tsx  # 知识卡片弹窗（底部滑入）
    NodeTouchNotice.tsx     # 节点首次触及底部通知（3.5s 自动消失）
    UnlockBanner.tsx   # 解锁通知横幅
    OutlineProgress.tsx     # 思想节点进度条
    SuggestedQuestions.tsx  # 推荐问题列表

  data/              # 纯数据，与渲染逻辑完全解耦
    philosophers-meta.ts    # PHILOSOPHERS 数组 + CONSTELLATIONS + PROVIDER_PRESETS
    philosophers/
      index.ts              # 统一导出 loadPhilosopher(id)
      socrates/
        index.ts            # 组装 PhilosopherModule
        outline.ts          # ThoughtNode[] — 思想节点数据
        lineage.ts          # lineageOrder + lineageConnections
        translations.ts     # TranslationFragment[] — 现代话题桥接
        frameworks.ts       # deepFrameworks Record
      plato/
        index.ts
        outline.ts          # 9个节点：cave/eidos/the_good/anamnesis/tripartite_soul/justice/philosopher_king/eros_ascent/immortality
        lineage.ts
        translations.ts
        frameworks.ts
      aristotle/
        index.ts
        outline.ts          # 9个节点：four_causes/substance/potentiality/eudaimonia/virtue/polis/logic/prime_mover/soul
        lineage.ts
        translations.ts
        frameworks.ts
        answers.ts          # 38条预设回答
      epicurus/
        index.ts
        outline.ts          # 9个节点：canon/atomism/cosmos/theology/hedone/ataraxia/death/tetrapharmakos/garden
        lineage.ts
        translations.ts
        frameworks.ts
        answers.ts          # 30条预设回答
        module.ts
      marcus/
        index.ts
        outline.ts          # 9个节点
        lineage.ts
        translations.ts
        frameworks.ts
        answers.ts
        module.ts
      augustine/
        index.ts
        outline.ts          # 9个节点：confession/divided_will/free_will/truth/evil/grace/rest_in_god/trinity/soul_love
        lineage.ts
        translations.ts
        frameworks.ts
        answers.ts + answers-part1.ts + answers-part2.ts
        module.ts
      aquinas/
        index.ts
        outline.ts          # 9个节点：five_ways/essence_existence/analogy/faith_reason/creation/soul/natural_law/virtue/beatitude
        lineage.ts
        translations.ts     # 5条现代话题桥接
        frameworks.ts       # 6个深入框架
        answers.ts + answers-part1.ts + answers-part2.ts   # 27条预设回答
        module.ts

  engine/            # 对话引擎，无 UI 依赖
    ai-client.ts     # callAI() — 统一 AI 调用层，支持 Anthropic/OpenAI-compat
    fragment-selector.ts    # selectFragments() — 关键词匹配思想节点
    question-generator.ts   # getSuggestedQuestions() — 生成推荐问题
    card-trigger.ts  # evaluateCardTrigger() — 知识卡片触发判断
    stage.ts         # getDialogueStage() — 对话阶段计算
    index.ts         # 统一导出

  prompts/           # AI prompt 构建
    socrates.ts      # buildPrompt() + BASE_PROMPT（含 [NODE:id] 标注指令）
    plato.ts         # buildPrompt() + BASE_PROMPT（含 [NODE:id] 标注指令）
    epicurus.ts      # buildEpicurusPrompt() + EPICURUS_BASE_PROMPT
    marcus.ts        # buildMarcusPrompt() + MARCUS_BASE_PROMPT
    augustine.ts     # buildAugustinePrompt() + AUGUSTINE_BASE_PROMPT
    aquinas.ts       # buildAquinasPrompt() + AQUINAS_BASE_PROMPT
    coordinate.ts    # 思维坐标旁白 prompt（每5轮触发）

  store/             # 全局状态
    app-store.ts     # useAppStore (Zustand)
    persistence.ts   # idbStorage 适配器（Zustand persist 用）

  types/             # TypeScript 类型定义
    philosopher.ts   # PhilosopherMeta, PhilosopherModule, ApiConfig, ProviderPreset
    thought-node.ts  # ThoughtNode, TranslationFragment, KnowledgeCard
    chat.ts          # ChatMessage, DialogueStage, FragmentSelection, AppView
    index.ts         # 统一 re-export

  App.tsx            # 顶层路由：根据 view 状态渲染不同页面
  main.tsx           # 入口，挂载 React
```

---

## 3. 核心数据模型

### PhilosopherModule（哲学家模块，新增哲学家的标准接口）
```typescript
interface PhilosopherModule {
  meta: PhilosopherMeta;              // 基础信息（名字、坐标、颜色等）
  outline: ThoughtNode[];             // 思想节点列表
  lineageOrder: string[];             // 推荐探索顺序（节点 id）
  lineageConnections: [number, number][]; // 思维导图连线（outline 下标对）
  translations: TranslationFragment[]; // 现代话题桥接片段
  deepFrameworks: Record<string, string>; // 深入框架说明
  basePrompt: string;                 // system prompt 基础文本
  buildPrompt: (...) => string;       // 动态构建完整 prompt
}
```

### ThoughtNode（思想节点，知识卡片的数据源）
- 每个节点代表哲学家思想的一个核心领域
- 包含：关键词、对话策略、问题集、知识卡片内容（card 字段）
- 对话引擎通过关键词匹配用户输入，确定当前激活的节点

### AppState（Zustand 全局状态）
**持久化字段**（IndexedDB，刷新后保留 — 通过 `partialize` 控制）：
- `philosophers` — 哲学家解锁状态
- `collectedCards` — 已收藏卡片 id 列表
- `exploredNodes` — 已深入节点 id 列表（hitCount ≥ 3）
- `chatCount` — 累计对话轮次（全局，跨 session）

**临时字段**（刷新后重置 — 对话是 session 级别的）：
- `messages`, `turnCount`, `recentMatchedNodes`, `usedQuestions`, `suggestedQs`
- `nodeHitCounts` — 节点命中次数（session 内，用于三阶段卡片触发）
- `touchNotice` — 底部触及通知 `{ nodeId, label } | null`
- `view`, `activePhilosopher`, `input`, `loading`
- `showThoughtMap`, `showCardNodeId`, `unlockNotif`

> ⚠️ `messages` / `turnCount` / `usedQuestions` **不持久化**，每次打开 App 是全新对话。

---

## 4. 核心流程

### 对话发送流程
```
用户输入 → doSend()
  → buildPrompt(text, prevMessages, exploredNodes, turnCount, recentMatchedNodes)
  → callAI(fullPrompt, apiMsgs, 1024)           # POST /api/chat
  → 解析 [NODE:id] tag，剥除后得 cleanReply
  → selectFragments(text + reply, ...)           # 关键词兜底匹配
  → effectiveNodeId = AI标签 优先，否则 关键词匹配结果
  → pushRecentMatch(effectiveNodeId)
  → evaluateCardTrigger({ matchedNodeId, nodeHitCounts, exploredNodes, outline })
      hitCount=1 → setTouchNotice() + setTimeout 3500ms 清除
      hitCount≥3 → markExplored() + collectCard() + setShowCardNodeId()（延迟1.5s）
  → incrementNodeHit(nodeId)
  → 每5轮：callAI(COORDINATE_PROMPT) → 追加 isCoordinate 消息
  → setMessages(finalMsgs)
  → setTimeout 300ms → getSuggestedQuestions() → setSuggestedQs()
  → 检查解锁条件 → setUnlockNotif()
```

### AI 调用层（重构后）
```
前端 ai-client.ts
  → POST /api/chat（我们自己的后端）
  → 携带：{ messages, philosopher_id }
  → 不携带任何 API Key

后端 server/routes/chat.ts
  → 读取 process.env.DEEPSEEK_API_KEY
  → POST https://api.deepseek.com/chat/completions
  → 将响应原样流式/一次性返回前端
```
- API Key **只存于** 服务器 `.env`，前端代码中无任何 Key 相关逻辑
- `ApiConfig` 类型和 `apiReady` Zustand 状态 **待清理**

### 持久化层
- `idbStorage`：实现 Zustand persist 接口的 IndexedDB 适配器
- 存储 key：`"dialogue-with-stars"`
- Schema version：1（预留迁移机制）
- Set<string> 序列化：存为 string[]，读取时转回 Set

---

## 5. 部署架构（Railway）

### 部署方式
- **平台**：Railway（railway.app），免备案，自动部署
- **构建**：Dockerfile（`node:20-alpine`），单阶段构建
- **前后端一体**：Express 在生产环境同时托管 Vite 构建产物（`dist/`）和 API 路由

### 关键配置文件
| 文件 | 作用 |
|------|------|
| `Dockerfile` | 基于 `node:20-alpine`，`npm install` → `npm run build` → `npx tsx server/index.ts` |
| `railway.toml` | 指定 `builder = "dockerfile"`，部署参数 |
| `.nvmrc` | 指定 Node 20（备用，主要靠 Dockerfile） |
| `.dockerignore` | 排除 `node_modules`、`dist`、`.env`、`.git` |

### 环境变量（Railway Variables）
| 变量名 | 值 | 说明 |
|--------|-----|------|
| `DEEPSEEK_API_KEY` | `sk-xxx` | DeepSeek API 密钥 |
| `NODE_ENV` | `production` | 启用静态文件托管 |
| `PORT` | Railway 自动分配 | 不需要手动设置 |

### 生产环境路由顺序（server/index.ts）
```
1. cors + json 中间件
2. app.use("/api", chatRouter)     ← POST /api/chat
3. app.get("/api/health", ...)     ← 健康检查
4. if (IS_PROD):
     express.static("dist/")       ← 静态资源
     app.get("{*path}", ...)       ← SPA fallback（注意 Express 5 语法）
5. app.listen(PORT, "0.0.0.0")    ← 必须监听 0.0.0.0
```

### 踩坑记录（Railway 首次部署）

| # | 问题 | 原因 | 修复方式 |
|---|------|------|---------|
| 1 | `Vite requires Node.js 20.19+` | Railway 默认 nixpacks 自动检测的 Node 版本过旧（18.x / 22.11） | 放弃 nixpacks，改用 Dockerfile 指定 `node:20-alpine` |
| 2 | `Cannot find native binding` (Rollup/Rolldown) | Windows 生成的 `package-lock.json` 不含 Linux 原生绑定 | 从 Git 移除 `package-lock.json`，加入 `.gitignore`，让容器内 `npm install` 自动生成 |
| 3 | Health check 超时 | Dockerfile 多阶段构建漏拷 `tsconfig.json` 等配置文件，tsx 启动崩溃 | 简化为单阶段 Dockerfile |
| 4 | Health check 仍然失败 | Express 只监听 `localhost`（127.0.0.1），容器外部无法访问 | `app.listen(PORT, "0.0.0.0")` 显式监听所有接口 |
| 5 | `Missing parameter name at index 1: *` | Express 5 使用新版 `path-to-regexp`，不再支持 `app.get("*")` | 改为 `app.get("{*path}")` |

> **教训**：Express 5 + Vite 8 + Docker 部署是全新的组合，很多 Express 4 时代的写法不再适用。注意检查 Express 5 的 [迁移指南](https://expressjs.com/en/guide/migrating-5.html)。

---

## 6. 扩展哲学家的标准步骤
1. 在 `src/data/philosophers/<id>/` 下创建 4 个数据文件（outline, lineage, translations, frameworks）
2. 创建 `index.ts` 组装 `PhilosopherModule`
3. 在 `src/prompts/<id>.ts` 编写 `basePrompt` 和 `buildPrompt`
4. 在 `src/data/philosophers-meta.ts` 的 `PHILOSOPHERS` 数组追加 `PhilosopherMeta`
5. 在 `CONSTELLATIONS` 追加连线关系
6. **主程序代码零修改**

---

## 6. 开发规范

- **TypeScript**：严格模式，禁用 any（除 IndexedDB 序列化边界处）
- **组件命名**：PascalCase，文件名与组件名一致
- **文件大小上限**：单文件不超过 300 行，超过则拆分
- **数据与 UI 分离**：components/ 不直接 import data/，通过 props 或 store 获取
- **禁止**：API Key 出现在任何前端文件、响应体、日志；`.env` 必须在 `.gitignore` 中
- **后端接口**：`POST /api/chat`，入参 `{ messages, philosopher_id }`，出参 AI 响应文本
- **Git 提交**：每个可验证的小功能完成后提交一次
